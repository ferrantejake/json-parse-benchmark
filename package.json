{
  "name": "json-parser-neon",
  "version": "0.1.0",
  "description": "",
  "main": "build/index.node",
  "scripts": {
    "clean": "rm -rf build/index.node && cargo clean",
    "test": "cargo test",
    "benchmark": "npm run build && node benchmark.js",
    "cargo-build": "cargo build --message-format=json-render-diagnostics > cargo.log",
    "postcargo-build": "mkdir -p build && neon dist -o build/index.node < cargo.log",
    "build": "npm run clean && npm run cargo-build -- --release",
    "debug": "npm run cargo-build --"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@neon-rs/cli": "0.1.82"
  }
}
